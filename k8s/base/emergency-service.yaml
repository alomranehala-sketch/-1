apiVersion: apps/v1
kind: Deployment
metadata:
  name: emergency-service
  namespace: healthcare
  labels:
    app: emergency-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: emergency-service
  template:
    metadata:
      labels:
        app: emergency-service
    spec:
      containers:
        - name: emergency-service
          image: ghcr.io/OWNER/REPO/emergency-service:latest
          ports:
            - containerPort: 3002
          envFrom:
            - configMapRef:
                name: healthcare-config
            - secretRef:
                name: healthcare-secrets
          env:
            - name: PORT
              value: '3002'
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: 3002
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/v1/health
              port: 3002
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: emergency-service
  namespace: healthcare
spec:
  selector:
    app: emergency-service
  ports:
    - port: 3002
      targetPort: 3002
